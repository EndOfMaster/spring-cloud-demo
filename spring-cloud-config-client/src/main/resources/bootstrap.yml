spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://127.0.0.1:8080
      username: test
      password: test
      fail-fast: true
      name: config-client
      profile: prod